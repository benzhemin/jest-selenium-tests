dist: bionic

addons:
  apt:
    sources:
      - google-chrome
    packages:
      -google-chrome-stable

language: node_js
node_js:
  - node
  - "lts/*"

before_install: npm i -g npm@latest

install: npm ci

script: npm test

cache: npm
